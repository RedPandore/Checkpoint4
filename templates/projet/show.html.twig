{% extends 'base.html.twig' %}

{% block title %}Projet{% endblock %}

{% block body %}
    <h1>Projet</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projet.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ projet.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ projet.url }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ projet.image }}</td>
            </tr>
            <tr>
                <th>Language</th>
                <td>{{ projet.language }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projet_index') }}" class="btn btn-primary">Liste</a>

    <a href="{{ path('projet_edit', {'id': projet.id}) }}" class="btn btn-primary">Éditer</a>

    {{ include('projet/_delete_form.html.twig') }}
{% endblock %}
