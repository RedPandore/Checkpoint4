{% extends 'layout.html.twig' %}

{% block title %}Competence{% endblock %}

{% block body %}
    <h1>Competence</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ competence.nom }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ competence.logo }}</td>
            </tr>
            <tr>
                <th>Other</th>
                <td>{{ competence.other ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('competence_index') }}">Liste</a>

    <a href="{{ path('competence_edit', {'id': competence.id}) }}">Éditer</a>

    {{ include('competence/_delete_form.html.twig') }}
{% endblock %}
